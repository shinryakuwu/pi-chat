<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <div>
      <strong><%= user.name %></strong>
      <div><%= user.username %></div>
      <div><%= user.bio %></div>
      <%= link_to "Profile", user_path(user) %>
    </div>
    <br>
  <% end %>
</div>
